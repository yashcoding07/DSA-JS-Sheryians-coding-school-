// Swap two numbers 
// Write a program that accepts two integers as input and swaps their values. After swapping, print the new values of the two numbers.

let a = 20;
let b = 10;
let temp;

temp = a;
a = b; 
b = temp;

console.log("a: " + a + " b: " +  b);