// Swap Two Numbers without using a Third Variable
// write a program which takes two numbers and swaps them without using the third variable.

a = 10;
b = 20;

[a, b] = [b, a];

console.log("a: " + a + " b: " + b);